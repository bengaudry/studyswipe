datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
  output   = "../src/db/generated/prisma"
}

enum UserPlan {
  FREE
  PREMIUM
}

model CasUser {
  id    String @id
  email String @unique

  isEmailVerified   Boolean
  name              String
  surname           String
  displayName       String
  profilePictureUrl String?

  // Payments
  stripeCustomerId String?
  plan             UserPlan @default(FREE)

  collections Collection[]
  decks       Deck[]

  casCreatedAt DateTime
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

//////    CUSTOM MODELS    \\\\\\

// An object grouping Decks of cards
model Collection {
  id      String @unique @default(uuid())
  ownerId String

  title String
  decks Deck[]

  createdAt DateTime? @default(now())
  updatedAt DateTime? @default(now())

  owner CasUser @relation(fields: [ownerId], references: [id], onDelete: Cascade)
}

// An object that contains a set of flashcards
model Deck {
  id           String @unique @default(uuid())
  collectionId String
  ownerId      String

  title       String
  description String?
  theme       String
  iconUri     String?
  isPublic    Boolean @default(false)
  cards       Json[]

  createdAt DateTime @default(now())
  updatedAt DateTime @default(now())

  Collection Collection @relation(fields: [collectionId], references: [id], onDelete: Cascade)
  Owner      CasUser    @relation(fields: [ownerId], references: [id], onDelete: Cascade, onUpdate: Cascade)
}
